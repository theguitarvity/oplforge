---
export interface Props {
  as?: 'div' | 'section' | 'article' | 'header' | 'footer' | 'main';
  class?: string;
}

const { as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8', className]}>
  <slot />
</Tag>
